# API base URLs are hardcoded in src/config/api-urls.ts (not read from env right now).
# Optional for local overrides if you switch code back to import.meta.env.VITE_* later:
# VITE_API_URL=https://umairnew.pythonanywhere.com/api
# VITE_API_BASE_URL=https://umairnew.pythonanywhere.com
